<%= form_with(model: post1) do |form| %>
  <% if post1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post1.errors.count, "error") %> prohibited this post1 from being saved:</h2>

      <ul>
        <% post1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
